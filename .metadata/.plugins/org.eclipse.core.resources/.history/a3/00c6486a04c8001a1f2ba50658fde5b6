
public class Ex5 {

	public static void main(String[] args) {
		/*
		 * 증감 연산자(++, --)
		 * -단항 연산자로 피연산자가 1개뿐인 연산자
		 * -피연산자(변수)의 값을 1만큼 증가(++)시키거나 감소(--)시킴
		 * -피연산자의 앞에 붙을 경우 전위(선행)연산자로 사용되며
		 * 	피연산자의 뒤에 붙을 경우 후위(후행)연산자로 사용됨
		 * -전위(선행)연산자 : 
		 * 피연산자의 값을 먼저 1만큼 증가시킨 뒤 다른 연산 등에 참여함
		 * -후위(후행)연산자 : 
		 * 피연산자를 먼저 다른 연산 등에 참여시킨 뒤 피연산자의 값을 1만큼 증가 또는 감소시킴
		 * 
		 */
		int a = 10;
		++a;//증감연산자를 사용하여 a값을 1만큼 증가시킴
		System.out.println(a);
		// =>a 값을 1만큼 증가시키는 다른 방법 : a = a + 1 or a +=1
		a++;
		System.out.println(a);
		--a;
		System.out.println(a);
		a--;
		System.out.println(a);
		
		System.out.println("<--------------------------->");
		
		int x = 5;
		int y = ++x; //전위(선행)연산자로 사용되는 증감연산자
		//먼저 x의 값을 1만큼 증가시킨 후 변수 y에 x값을 대입한다
		//따라서 x가 5->6으로 변화된 후 y에 저장되므로 y도 6이 저장
		System.out.println(x+","+y);
		int x2 = 5;
		int y2 = x2++; //후위(후행)연산자로 사용되는 증감연산자
		//먼저 x의 값을 변수 y2에 대입한 뒤 x2의 값을 1만큼 증가시킴
		//따라서 x2가 5일때 y2에 저장되고
		//그 후 x2의 값이 5->6으로 증가
		System.out.println(x2+","+y2);
		
		
		
		
		
		
	}

}
